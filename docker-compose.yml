version: '3.8'

services:
  balancer:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - backend1
      - backend2
    environment:
      - PORT=8080

  backend1:
    image: nginx
    ports:
      - "8081:80"

  backend2:
    image: nginx
    ports:
      - "8082:80"